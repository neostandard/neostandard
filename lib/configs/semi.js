// TODO: Use eslint-stylistic

/** @satisfies {import('@typescript-eslint/utils/ts-eslint').FlatConfig.Config} */
export const semiConfig = {
  rules: {
    semi: ['error', 'always'],
    'no-extra-semi': 'error',
  },
}
