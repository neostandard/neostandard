// @stylistic 5.4 (eslint-stylistic #933) started policing the indent of the
// RHS after a line-broken `=` (new `assignmentOperator` option, default 1).
// standard never had an opinion here, so neostandard sets it to 'off' — this
// fixture's unusual continuation indents must stay accepted.
const looselyIndented =
      'this continuation indent matches neither 2 nor 4'

export const alsoLoose =
  looselyIndented
